{
  "name": "notification-service",
  "projectType": "application",
  "sourceRoot": "apps/notification-service/src",
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/notification-service",
        "main": "apps/notification-service/src/main.ts",
        "tsConfig": "apps/notification-service/tsconfig.app.json",
        "platform": "node",
        "format": ["cjs"],
        "bundle": true,
        "external": ["@nestjs/*", "@grpc/*", "grpc", "rxjs"],
        "esbuildConfig": "apps/notification-service/esbuild.config.js"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node dist/apps/notification-service/main.cjs",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
